@extends('layouts.app') @section('title','Conciliações') @section('section','Financeiro') @section('page-title','Conciliações')
@section('content')<div class="page-heading"><div><span class="eyebrow">Conferência financeira</span><h2>Conciliações</h2><p>Compare pagamentos, recebimentos e movimentos realizados.</p></div>@can('payments')<a class="button primary" href="{{ route('reconciliations.create') }}">+ Nova conciliação</a>@endcan</div><section class="panel table-panel"><form class="filter-bar"><label>Conta<select name="account"><option value="">Todas</option>@foreach($accounts as $a)<option value="{{ $a->id }}" @selected(request('account')==$a->id)>{{ $a->nome }}</option>@endforeach</select></label><label>Status<select name="status"><option value="">Todos</option>@foreach(['ABERTA','ABERTO','ARQUIVADO'] as $s)<option @selected(request('status')===$s)>{{ $s }}</option>@endforeach</select></label><button class="button primary">Filtrar</button></form><div class="table-wrap"><table><thead><tr><th>Período</th><th>Conta</th><th>Competência</th><th>Status</th><th></th></tr></thead><tbody>@forelse($records as $r)<tr><td>{{ $r->dateLabel('data_inicial') }} — {{ $r->dateLabel('data_final') }}</td><td>{{ $r->id_conta }}</td><td>{{ str_pad((string)$r->mes,2,'0',STR_PAD_LEFT) }}/{{ $r->ano }}</td><td><span class="badge {{ mb_strtoupper($r->status)==='ARQUIVADO'?'neutral':'warning' }}">{{ $r->status }}</span></td><td><a class="row-action" href="{{ route('reconciliations.show',$r) }}">→</a></td></tr>@empty<tr><td colspan="5"><div class="empty-state"><strong>Nenhuma conciliação</strong></div></td></tr>@endforelse</tbody></table></div>@include('partials.pagination',['paginator'=>$records])</section>@endsection
