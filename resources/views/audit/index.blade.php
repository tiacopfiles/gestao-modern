@extends('layouts.app') @section('title','Auditoria') @section('section','Administração') @section('page-title','Auditoria')
@section('content')<div class="page-heading"><div><span class="eyebrow">Rastreabilidade</span><h2>Auditoria</h2><p>Histórico de inclusões, alterações, exclusões, parcelas e documentos.</p></div></div><section class="panel table-panel"><form class="filter-bar"><label class="search"><span>⌕</span><input name="table" value="{{ request('table') }}" placeholder="Tabela"></label><label>Ação<input name="action" value="{{ request('action') }}"></label><label>De<input type="date" name="from" value="{{ request('from') }}"></label><label>Até<input type="date" name="to" value="{{ request('to') }}"></label><button class="button primary">Filtrar</button></form><div class="table-wrap"><table><thead><tr><th>Data</th><th>Tabela</th><th>Registro</th><th>Ação</th><th>Usuário</th></tr></thead><tbody>@forelse($records as $r)<tr><td>{{ $r->dateLabel('data') }}</td><td>{{ $r->nome_tabela }}</td><td>#{{ $r->registro }}</td><td><span class="badge info">{{ $r->tipo_alteracao }}</span></td><td>{{ $users[$r->id_usuario] ?? 'Usuário '.$r->id_usuario }}</td></tr>@empty<tr><td colspan="5"><div class="empty-state"><strong>Nenhum evento registrado</strong></div></td></tr>@endforelse</tbody></table></div>@include('partials.pagination',['paginator'=>$records])</section>@endsection
